NEXUS ROYALE — DELTA V84 (LEGGI QUESTO: risolve "le modifiche non arrivano")
=============================================================================
Sostituisci: game.js  sw.js  index.html      <-- index.html e' NUOVO, serve!

PERCHE' LA V83 SEMBRAVA NON AVER FATTO NULLA
Le modifiche c'erano davvero nel file, ma il tuo telefono non le ha MAI caricate.
Il service worker era scritto cosi': "cerca prima nella cache, vai in rete solo se
non trovi". Quindi finche' la vecchia cache esisteva ti serviva il game.js VECCHIO,
per sempre, anche dopo aver messo i file nuovi online. Non era un problema del gioco.

COSA CAMBIA ORA (una volta sola, poi non succede piu')
- sw.js: il CODICE (game.js, assets_fallback.js, index.html) ora e' NETWORK-FIRST:
  si scarica sempre la versione nuova, e la cache resta solo come rete di sicurezza
  offline. Le IMMAGINI restano cache-first, quindi il gioco resta veloce.
- index.html: gli script hanno la versione nel nome (game.js?v=84) e la pagina si
  ricarica DA SOLA una volta quando trova una build nuova.
- Dalla prossima build ti bastera' sostituire i file: niente piu' balletti con la cache.

PRIMA VOLTA (serve per uscire dalla cache vecchia): apri il sito, tira giu' per
ricaricare 2 volte. Se proprio resiste: impostazioni sito -> cancella dati, oppure
disinstalla e reinstalla la PWA. Da qui in avanti non servira' piu'.

FIX ANCHE: il rumore fastidioso che continuava mentre stavi sopra un'arma con lo
zaino pieno (il bip partiva a ogni frame). Ora suona al massimo una volta al secondo.

DENTRO CI SONO ANCHE LE COSE DELLA V83 (che non hai mai visto):
  - barra ZAINO con vera area cliccabile -> si apre
  - HUD riordinato: vita/scudo -> ZAINO a sinistra -> arma+AUTO -> icona audio in alto a destra
