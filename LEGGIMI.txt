NEXUS ROYALE v2.0.1 — CORREZIONE   (a schermo: BUILD v2.0.1)
=============================================================
Sostituisci SOLO questi 4 file:  game.js  sw.js  index.html  assets_fallback.js
Gli asset li hai gia' dal pacchetto completo. NON serve ricaricare tutto.

COSA ERA SUCCESSO
"this.drawRoads is not a function": ripulendo i vecchi asset dal preload, il mio
script ha tagliato piu' del dovuto e ha portato via con se' sei metodi interi
(drawRoads, realBuilding, tileBuilding, roofProps, openBuilding, furnishReal).
Li ho recuperati e rimessi al loro posto.

E DENTRO C'ERA LA SORPRESA
Uno dei metodi recuperati e' roofProps: e' LUI che disegnava le VENTOLE (fan1-4) e i
pannelli solari (sol1-2) sui tetti. Non l'avevo mai visto perche' non lo cercavo li'.
ORA E' SPENTO. Insieme a lui ho spento anche:
 - il vecchio costruttore di edifici a pezzi (b_*)
 - il terreno di fondo vecchio (sw_f) -> ora usa il tuo pz_floor
 - il pavimento interno vecchio (fl_n) -> ora usa il tuo if_home

Il codice vecchio resta nel file ma e' IRRAGGIUNGIBILE (i metodi escono subito):
non puo' piu' disegnare nulla. Se vuoi lo cancello del tutto in un secondo momento,
con calma e verificando build per build.
