NEXUS ROYALE — DELTA V93  ·  FIX BLOCCO ALL'AVVIO   (a schermo: BUILD v93)
===========================================================================
Sostituisci: game.js  sw.js  index.html

PERCHE' SI BLOCCAVA (non era la minimappa): con la citta' nuova i blocchi edificabili
sono passati da ~8 a piu' di 90. Ogni palazzo grande genera stanze, arredi e una
collisione per ogni mobile -> decine di migliaia di collisioni. La funzione che cerca
un punto libero (per loot, props, atterraggio) le scorreva TUTTE, ogni volta, per
centinaia di volte: il gioco si impiantava prima ancora di partire.

FIX:
1) Tetto alle megastrutture: massimo 14 palazzi "grandi" con interni per partita,
   scelti a caso fra i blocchi disponibili. Gli altri blocchi restano palazzi normali,
   quindi la citta' resta piena ma non esplode.
2) La ricerca del punto libero ora usa un indice a griglia: guarda solo le collisioni
   della cella dove sta cercando, non tutte quante. E' centinaia di volte piu' veloce.
3) Ridotti i tentativi per le props della citta' (erano 420 ricerche, ora 90).

Se dovesse essere ancora lento all'avvio, dimmelo e abbasso MAX_MEGA (riga ~2215).
