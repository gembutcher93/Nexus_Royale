NEXUS ROYALE — DELTA V87
=========================
Sostituisci: game.js  sw.js  index.html      (in alto a schermo leggerai BUILD v87)

1) ICONA AUDIO spostata in alto al centro, tra la barra della salute e VIVI/KILL,
   piu' a portata di pollice.

2) SCHERMO INTERO: al PRIMO tocco sullo schermo il gioco va a tutto schermo e
   sparisce la barra dell'indirizzo. I browser lo consentono solo dopo un gesto
   dell'utente, per questo non puo' partire da solo al caricamento.
   Se lo apri dalla PWA installata (icona in home) parte gia' senza barra.
   Provo anche a bloccare l'orientamento in verticale dove il browser lo permette.
